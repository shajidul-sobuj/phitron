#include <bits/stdc++.h>
using namespace std;

int main() {
    int x;
    while(cin >> x)
    {
        if (x==0)
            return 0;
        if (x==1)
        {
            cout << 1 << endl;
            continue;
        }
        
        
    }
    return 0;
}