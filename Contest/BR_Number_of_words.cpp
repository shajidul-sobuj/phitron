#include <bits/stdc++.h>
using namespace std;
int main() {
    char s[1001];
    cin.getline(s,1001);
    int sz = strlen(s);
    int count=0;
    for (int i=0;i<sz;i++) {
        if (s[i]!=' '  && ) {
            count++;
        }
    }
    cout <<  count << endl;

    return 0;
}